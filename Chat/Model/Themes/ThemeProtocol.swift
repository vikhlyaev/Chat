import UIKit

protocol ThemeProtocol {
    var backgroundColor: UIColor { get }
    var secondaryBackgroundColor: UIColor { get }
    var textColor: UIColor { get }
    var tintColor: UIColor { get }
    var profileScreenBackgroundColor: UIColor { get }
    var settingsScreenBackgroundColor: UIColor { get }
    var settingsCellBackgroundColor: UIColor { get }
}
